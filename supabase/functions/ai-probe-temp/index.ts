// ============================================================================
// ai-probe-temp — مُعطّلة.
// ----------------------------------------------------------------------------
// كانت دالة تشخيص مؤقتة لفحص مضيفات AgentRouter وترويسات المصادقة.
// انتهى غرضها، ولم يعد فيها أي منطق يلمس المفاتيح أو قاعدة البيانات.
// يُفضّل حذفها نهائيًا من Supabase Dashboard ← Edge Functions.
// ============================================================================
import "jsr:@supabase/functions-js/edge-runtime.d.ts";

Deno.serve(() =>
    new Response(
        JSON.stringify({ error: "gone", message: "دالة تشخيص مؤقتة انتهى غرضها — احذفها من لوحة Supabase" }),
        { status: 410, headers: { "Content-Type": "application/json" } },
    )
);
